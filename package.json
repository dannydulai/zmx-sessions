{
  "name": "zmx-sessions",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "zmx-sessions": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile zmx-sessions",
    "test": "bun test",
    "fmt": "bunx biome format --write src/",
    "lint": "bunx biome check src/"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "string-width": "^7.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.5.2"
  }
}

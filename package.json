{
  "name": "zsm",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "zsm": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile zsm-bun",
    "test": "bun test",
    "fmt": "bunx biome format --write src/",
    "lint": "bunx biome check src/"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "string-width": "^7.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
